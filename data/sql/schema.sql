CREATE TABLE chpu_main (id BIGINT AUTO_INCREMENT, url VARCHAR(255) NOT NULL UNIQUE, short VARCHAR(255) NOT NULL UNIQUE, created_at DATETIME NOT NULL, updated_at DATETIME NOT NULL, PRIMARY KEY(id)) ENGINE = INNODB;
